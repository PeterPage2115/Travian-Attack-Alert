{
  "schemaVersion": 1,
  "scope": "Legacy monolith size debt in src/runtime.js only. Every other src/ module must satisfy the new-module limit.",
  "newModuleMaxPureLines": 250,
  "monolith": {
    "path": "src/runtime.js",
    "maxPureLines": 10774,
    "note": "Strictly non-increasing budget: actual pure lines must be <= maxPureLines. Any increase fails the gate. Task 15 (legacy queue reconciliation) recorded +203 debt from 10455; Task 20 (same-document pre-scan lease reacquisition) recorded the required +16 from 10658; Task 21 (bounded current-world attack-memory reset through the existing envelope writer) recorded the required +90 from 10674; Task 22 (resume a live multi-payload chunk retry at the first unacknowledged payload) recorded the required +10 from 10764. No headroom remains."
  }
}
